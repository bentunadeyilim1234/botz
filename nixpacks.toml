[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npx -y pnpm@9 install", "npx playwright install chromium --with-deps"]

[phases.build]
cmds = ["npx -y pnpm@9 run build"]

[start]
cmd = "npm start"
